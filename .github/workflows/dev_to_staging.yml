name: Dev to Staging (CI/CD)

on:
<<<<<<< HEAD
  pull_request:
    branches:
      - staging
    types: [closed]

jobs:
  train-evaluate-promote:
    if: github.event.pull_request.merged == true
=======
  push:
    branches:
      - staging

jobs:
  train-evaluate-promote:
>>>>>>> feature/training_and_evaluation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .
<<<<<<< HEAD
          pip install pytest mlflow dagshub "dvc[gdrive]" scikit-learn

      # --- DVC (Google Drive Service Account) ---
=======
          pip install pytest mlflow dagshub "dvc[gdrive]"

      # --- DVC (Google Drive) ---
>>>>>>> feature/training_and_evaluation
      - name: Write GDrive service account key
        run: |
          mkdir -p .secrets
          echo '${{ secrets.GDRIVE_SERVICE_ACCOUNT_JSON }}' > .secrets/gdrive-sa.json

      - name: Configure DVC for GDrive (service account)
        run: |
          dvc remote modify gdrive gdrive_use_service_account true --local
          dvc remote modify gdrive gdrive_service_account_json_file_path .secrets/gdrive-sa.json --local

      - name: Fetch real data with DVC
        run: |
          dvc pull

      # --- Tests ---
      - name: Run unit tests
        run: |
<<<<<<< HEAD
          python -m pytest -q

      # --- Training (MLflow enabled here!) ---
      - name: Train on staging subset
=======
          pytest -q

      # --- Training ---
      - name: Train on a larger subset
>>>>>>> feature/training_and_evaluation
        env:
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.MLFLOW_TRACKING_USERNAME }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.MLFLOW_TRACKING_PASSWORD }}
        run: |
          python training/train.py \
            --train_json data/train-v2.0.json \
            --val_json data/dev-v2.0.json \
            --epochs 3 \
            --batch_size 16 \
            --subset_train 20000 \
            --subset_val 4000

<<<<<<< HEAD
      # --- Quality gate + Promotion + Tags ---
      - name: Quality Gate & Promote model to Staging (with tags)
        env:
          F1_THRESHOLD: "0.10"
=======
      # --- Quality gate + Promotion to Staging ---
      - name: Quality Gate (F1 Threshold) & MLflow Staging Promotion
        env:
          F1_THRESHOLD: "0.60"
>>>>>>> feature/training_and_evaluation
          MODEL_NAME: "qa_model"
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.MLFLOW_TRACKING_USERNAME }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.MLFLOW_TRACKING_PASSWORD }}
<<<<<<< HEAD
          GIT_SHA: ${{ github.sha }}
          GIT_REF: ${{ github.ref }}
        run: |
          python - << 'PY'
          import json, os, sys
          from datetime import datetime, timezone
=======
        run: |
          python - << 'PY'
          import json, os, sys
>>>>>>> feature/training_and_evaluation
          import mlflow
          from mlflow.tracking import MlflowClient

          mlflow.set_tracking_uri(os.environ["MLFLOW_TRACKING_URI"])
          client = MlflowClient()

          threshold = float(os.environ["F1_THRESHOLD"])
          model_name = os.environ["MODEL_NAME"]

          metrics_path = "artifacts/metrics.json"
          if not os.path.exists(metrics_path):
              print("Metrics file not found:", metrics_path)
              sys.exit(1)

          with open(metrics_path, "r", encoding="utf-8") as f:
              metrics = json.load(f)

          best_f1 = float(metrics.get("best_val_f1", 0.0))
          print(f"Obtained F1 Score: {best_f1:.4f}")

          if best_f1 < threshold:
              print(f"Quality Gate failed: {best_f1:.4f} < {threshold:.2f}")
              sys.exit(1)

<<<<<<< HEAD
          # Promote latest NONE stage version
          versions = client.get_latest_versions(model_name, stages=["None"])
          if not versions:
=======
          print("Quality Gate passed! Promoting model to Staging...")

          # Prefer promoting the latest NONE stage version
          versions = client.get_latest_versions(model_name, stages=["None"])
          if not versions:
              # fallback: promote latest version if 'None' isn't returned by backend
>>>>>>> feature/training_and_evaluation
              all_versions = client.search_model_versions(f"name='{model_name}'")
              if not all_versions:
                  print("No model versions found.")
                  sys.exit(1)
<<<<<<< HEAD
              mv = max(all_versions, key=lambda v: int(v.version))
          else:
              mv = versions[0]

          version = mv.version
          run_id = mv.run_id

          client.transition_model_version_stage(
              name=model_name,
              version=version,
=======
              latest = max(all_versions, key=lambda v: int(v.version))
              latest_version = latest.version
          else:
              latest_version = versions[0].version

          client.transition_model_version_stage(
              name=model_name,
              version=latest_version,
>>>>>>> feature/training_and_evaluation
              stage="Staging",
              archive_existing_versions=True
          )

<<<<<<< HEAD
          now = datetime.now(timezone.utc).isoformat()
          client.set_model_version_tag(model_name, version, "promoted_to", "staging")
          client.set_model_version_tag(model_name, version, "quality_gate_f1", f"{best_f1:.4f}")
          client.set_model_version_tag(model_name, version, "promoted_by", "github-actions")
          client.set_model_version_tag(model_name, version, "git_sha", os.environ.get("GIT_SHA", ""))
          client.set_model_version_tag(model_name, version, "git_ref", os.environ.get("GIT_REF", ""))
          client.set_model_version_tag(model_name, version, "promoted_at_utc", now)

          if run_id:
              client.set_tag(run_id, "promoted_to", "staging")
              client.set_tag(run_id, "quality_gate_f1", f"{best_f1:.4f}")
              client.set_tag(run_id, "git_sha", os.environ.get("GIT_SHA", ""))
              client.set_tag(run_id, "git_ref", os.environ.get("GIT_REF", ""))

          print(f"✅ Model promoted to Staging: {model_name} v{version}")
          PY
=======
          print(f"Model promoted to Staging (Version {latest_version})!")
          PY
>>>>>>> feature/training_and_evaluation
